```python
import modeanalytics_dbapi

connection = modeanalytics_dbapi.connect(
  placeholder_report_name="Personal/placeholder report",
  access_token=ACCESS_TOKEN,
  access_token_password=ACCESS_TOKEN_PASSWORD,
  organization=ORGANIZATION,
)

```
