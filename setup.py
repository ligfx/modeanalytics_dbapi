from setuptools import setup

setup(
  name='modeanalytics_dbapi',
  version='0.1.0',
  url='https://github.com/ligfx/modeanalytics_dbapi',
  py_modules=['modeanalytics_dbapi'],
  install_requires=['requests'],
  license='MIT',
  zip_safe=True,
)
